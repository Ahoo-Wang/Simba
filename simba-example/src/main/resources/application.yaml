simba:
  jdbc:
    enabled: true
#  redis:
#    enabled: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/simba_db
    username: root
    password: root
  redis:
    url: redis://localhost:6379
  task:
    scheduling:
      pool:
        size: 2


logging:
  level:
    me.ahoo.simba: debug
